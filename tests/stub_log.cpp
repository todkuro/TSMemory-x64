//----------------------------------------------------------------------------
//	テストから字幕のコードを直接呼ぶ為の受け皿
//
//	`src/aviutl2/caption/` の一部は plugin_main.h のログ関数を呼ぶ。
//	本体 (AviUtl2 のロガー) はテストには無いので、ここで空の実体を出す。
//	**プラグインのビルドには含めない** (本物と衝突する)。
//----------------------------------------------------------------------------
#include <windows.h>

#include "plugin_main.h"

void TSMemoryLog(LPCWSTR) {}
void TSMemoryLogWarn(LPCWSTR) {}
