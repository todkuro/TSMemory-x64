# 更新履歴

このファイルが**版の唯一の正**です。
`tools/build.sh` が一番上の `## ver.X.Y.Z` を読み取り、

- AviUtl2 のプラグイン一覧に出る版 (`TSMEMORY_VERSION_TEXT`)
- パッケージの `package.ini` / `package.txt`

に流し込みます。**リリース時に書き換えるのはこのファイルだけです。**

## ver.0.3.2 (字幕対応)

字幕 (ARIB STD-B24) の取り込みに対応しました。**既定は無効**で、
有効にしない限り字幕側のコードは一切動きません。
設定と手順は [docs/README_Subtitles.md](docs/README_Subtitles.md) を参照してください。

- 字幕を AviUtl2 の**テキストオブジェクト**としてタイムラインに並べる。
  焼き込みではないので、取り込んだ後に書体も色も文言も直せる
- 放送は行ごとに座標を持っている為、**1 行につき 1 つのオブジェクト**を
  作り、レイヤーを分けて並べる (同じレイヤーには時間の重なる
  オブジェクトを置けない為)
- 放送と同じ半透明の黒い箱と文字の縁取りを、同梱のスクリプト
  「TSMemory字幕背景」で再現する。**描画後の実寸を見て敷く**ので、
  後からフォントや文言を変えても箱が付いて来る
- ルビは `</>漢字<!>ふりがな</>` の形にまとめる
- 文字色・背景色・文字サイズ・太字・斜体を、AviUtl2 の制御文字に直す。
  書体は `[Caption] Preset` に書いたテキストプリセットで一括して変えられる
- 表示の長さは、放送が送って来る「画面消去」のデータユニットから決める
- 表示領域 (SDF) の右端で折り返す
- 外字 (DRCS) は対応表で本物の文字に置き換える。放送の外字の多くは
  Unicode に実在する文字 (`｟｠『』〜㊙`、`德 髙 﨑 塚 𠮷` 等) を
  ビットマップで送っているだけなので、この分は**その回の取り込みから**
  普通の文字として出る。編集も検索もでき、縁取りも色も効く。
  利用者が `TSMemoryDrcsMap.txt` に足す事もできる
- 対応表に無い字形は貯めておき、次の起動時にフォントとして登録する。
  AviUtl2 がフォントの登録を初期化中しか受け付けない為、その回だけ
  代替文字になり、次の起動から本来の字で出る
- 字形は数十秒おきにしか流れて来ない為、TVTest 側で字幕だけを別に
  長く溜めておいて拾う (`[Settings] CaptionMemorySize`、既定 1MB)
- TVTest 側の `[Settings] Subtitle` の既定を 1 にした。字幕は取り込む量の
  0.01〜0.02% しか無く、通しておけば AviUtl2 側の設定だけで
  切り替えられる為 (TVTest 側の変更には TVTest の再起動が要る)
- **文字スーパー**(緊急放送等) は取り込まない。**下線と囲み**は
  AviUtl2 のテキストに文字スタイルとして無い為に出せない。
  GSM (文字変形) と CCC (合成) は参照実装が 2 つとも未対応の為、
  復号して読み飛ばすだけにしてある
- 音声と字幕の説明を `docs/README_Audio.md` /
  `docs/README_Subtitles.md` に分けた (どちらも既定で無効な任意機能の為)

## ver.0.3.1

- H.264 / H.265 の映像と、4K8K の音声 (LATM / MPEG-4 raw) を取り込み
  対象に加えた。AviUtl2 側では復号できないが、**TVTest 側で捨てると
  後段で取り返せない**為に残す
- 復号できない形式の音声だった場合は「音声はありますが対応していない
  形式です (stream_type 0x11)」とログに出すようにした。「音声が無い」と
  「形式が違って読めない」を取り違えると、設定を疑わせてしまう為

## ver.0.3.0 (64bit / AviUtl ExEdit2 対応版)

- 64bit ビルドに対応し、AviUtl ExEdit2 で動作するようにした
- AviUtl 側の `TVTestSrc.aui` と `CaptureUtil.auf` を、AviUtl2 の汎用
  プラグイン `TSMemory-TVTestSrc.aux2` ひとつにまとめた
- 画像の保存に Windows 標準の WIC を使うようにした
  (`TVTest_Image.dll` が不要になり、png / jpeg / bmp / tiff に対応)
- マルチ編成でサブチャンネルを視聴している時に、そのチャンネルの映像を
  取り込むようにした (従来はプライマリチャンネルの映像しか取れなかった)
- 取り込んだ映像にフィルタのプリセットを自動適用する設定を追加した
- 終了時の保存確認に自動応答する設定を追加した
- 実行の度に別名の共有メモリを作るようにした (AviUtl2 のキャッシュ対策)
- 取り込み後のシーク位置と、配置先レイヤーのロックを設定できるようにした
- 音声 (AAC) の取り込みに対応した。**既定は無効**で、TVTest 側と
  AviUtl2 側の**両方**の設定が要る。AAC (ADTS) を Windows 標準の
  Media Foundation で復号し、映像との時間差は PTS を見て詰める。
  設定と手順は [docs/README_Audio.md](docs/README_Audio.md) を参照

---

ver.0.2.1 以前の更新履歴は、AviUtl 1.xx 版の `TSMemory.txt` を参照してください。
